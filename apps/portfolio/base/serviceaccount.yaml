apiVersion: v1
kind: ServiceAccount
metadata:
  name: portfolio
  labels:
    app.kubernetes.io/name: portfolio-portfolio
    app.kubernetes.io/component: web
# The app never calls the Kubernetes API, so it gets no token mounted.
automountServiceAccountToken: false
